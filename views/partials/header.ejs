 <%
    var name = 'Unknown';
    var photo = "images/signedout.png";
    var userID;
  if(locals.user){
    userID = locals.user._id;
    if(locals.user.facebook.name != null){
      name = locals.user.facebook.name;
      photo = locals.user.facebook.photo;
    }else if(locals.user.twitter.username != null){
      name = locals.user.twitter.username;
      photo = locals.user.twitter.photo;
    }else if(locals.user.google.name != null){
      name = locals.user.google.name;
      photo = locals.user.google.photo;
    }else if(locals.user.local.email != null){
      name = locals.user.local.email;
      if(locals.user.local.avatar != null){
        photo = local.user.local.avatar;
      }else{
        photo = "images/signedout.png";
      }
    }
  }
 %>



<nav class="navbar navbar-expand navbar-dark bg-primary">
  <a class="navbar-brand abs" id="quizheadername" href="/console"><%= title %></a>
  
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsingNavbar">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="navbar-collapse collapse" id="collapsingNavbar">
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" href=""></a>
      </li>

      <li class="nav-item">
        <a class="nav-link" href="#myAlert" data-toggle="collapse"></a>
      </li>
    </ul>
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
       <!-- <a class="nav-link nav-big" href="/scoreboard">Scoreboard</a>-->
      </li>
    </ul>
    

<% if(locals.user){ %>
    <div class="dropdown">
  
     <button class="dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="fa fa-bars fa-6x"></span>
      </button> 


      <ul class="dropdown-menu dropdown-left" id="locationGameMenu"  aria-labelledby="dropdownMenuButton">
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="/console">Console</a><span class="fa fa-dashboard"></span>
        <hr />
        <a class="dropdown-item" href="/buyproperties">Buy Properties</a><span class="fa fa-building"></span>
        <hr />
        <a class="dropdown-item" href="/portfolio/<%= userID %>">Portfolio</a><span class="fa fa-briefcase"></span>
        <hr />
        <a class="dropdown-item" href="/scoreboard">Scoreboard</a><span class="fa fa-group"></span>
        <hr />
        <a class="dropdown-item" href="/profile">Your Profile</a><span class="fa fa-gear"></span>
        <hr />
        <% if(locals.user.permissions.admin){ %>
        <a class="dropdown-item" href="/admin">Admin</a><span class="fa fa-wrench"></span>
        <% } %>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="/logout">Logout</a><span class="fa fa-circle"></span>
      </ul>
    </div>

<% }else{ %>


    <div class="dropdown">
     <button class="dropdown-toggle" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <span class="fa fa-bars fa-6x"></span>
      </button> 

      <ul class="dropdown-menu dropdown-left" id="locationGameMenu" aria-labelledby="dropdownMenuButton">
        <a class="dropdown-item" href="/login">Login</a><span class="fa fa-circle-thin"></span>
        <div class="dropdown-divider"></div>
        <a class="dropdown-item" href="/scoreboard">Scoreboard</a><span class="fa fa-group"></span>
      </ul>
    </div>

<% } %>

  </div>
</nav>





